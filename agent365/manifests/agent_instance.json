{
  "displayName": "${AGENT_IDENTITY_DISPLAY_NAME}",
  "description": "Task analysis and action planning through the Next Best Action MCP service. Registry publication is metadata only; identity permissions and runtime approvals are configured separately.",
  "ownerIds": [
    "${AGENT_REGISTRY_OWNER_ID}"
  ],
  "originatingStore": "${AGENT_REGISTRY_ORIGINATING_STORE}",
  "sourceAgentId": "${AGENT_REGISTRY_SOURCE_AGENT_ID}",
  "agentIdentityId": "${AGENT_IDENTITY_APP_ID}"
}
